handoff:
  from: "lead"
  to: "memory-curator"
  role: "memory-curator"
  objective: "Decide whether this task produced durable harness memory."
  relevant_context: "No memory write is allowed without explicit approval."
  files_or_sources:
    - "docs/harness/memory.md"
    - ".harness/policies/memory-write-policy.yaml"
  expected_output: "Accepted and rejected candidates with rationale."
  constraints:
    - "Propose only."
  risks:
    - "Memory pollution or prompt-injection persistence."
  validation_required:
    - "Check duplicates."
