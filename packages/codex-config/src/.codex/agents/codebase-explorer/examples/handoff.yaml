handoff:
  from: "lead"
  to: "codebase-explorer"
  role: "codebase-explorer"
  objective: "Map the repository files relevant to Codex harness configuration."
  relevant_context: "Do not edit files. Do not invent commands."
  files_or_sources:
    - "AGENTS.md"
    - ".codex/"
    - ".harness/"
  expected_output: "Repository shape, relevant files, risks, and unknowns."
  constraints:
    - "Read-only."
  risks:
    - "Config files may be runtime-sensitive."
  validation_required:
    - "Cite inspected files."
