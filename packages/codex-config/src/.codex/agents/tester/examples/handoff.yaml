handoff:
  from: "lead"
  to: "tester"
  role: "tester"
  objective: "Validate Codex agent preset configuration syntax."
  relevant_context: "Only run safe static checks."
  files_or_sources:
    - ".codex/config.toml"
    - ".codex/agents/*.toml"
  expected_output: "Commands run, results, and validation gaps."
  constraints:
    - "Do not edit files."
  risks:
    - "Validation tools may be unavailable."
  validation_required:
    - "Parse TOML."
