handoff:
  from: "lead"
  to: "reviewer"
  role: "reviewer"
  objective: "Review Codex agent preset directory normalization."
  relevant_context: "Do not move .codex/agents/*.toml runtime entrypoints."
  files_or_sources:
    - ".codex/agents/"
    - "README.md"
    - ".harness/manifest.yaml"
  expected_output: "Blocking and non-blocking issues with evidence."
  constraints:
    - "Read-only review."
  risks:
    - "Discovery can break if TOML files move."
  validation_required:
    - "TOML parse check."
    - "Manifest coverage check."
