{
  "name": "@liushi-agent-settings/codex-config",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "files": [
    "src",
    "scripts",
    "tsconfig.hooks.json"
  ],
  "scripts": {
    "build:hooks": "tsc -p tsconfig.hooks.json && node scripts/fix-hook-output-extensions.mjs",
    "check:hooks": "tsc -p tsconfig.hooks.json --noEmit"
  }
}
