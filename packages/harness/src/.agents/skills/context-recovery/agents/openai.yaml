interface:
  display_name: "Context Recovery"
  short_description: "Focused repository exploration before planning or editing."
  default_prompt: "Use $context-recovery to recover the relevant project context before making changes."
policy:
  allow_implicit_invocation: true
